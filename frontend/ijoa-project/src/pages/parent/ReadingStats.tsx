import Histogram from "../../components/parent/stats/Histogram";

const ReadingStats = () => {
  return (
    <div className="px-20 pt-24 pb-10 h-screen">
      <Histogram />
    </div>
  );
};

export default ReadingStats;
